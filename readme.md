A mapset maker designed for merging maps from a tournament round into a single mapset, allowing more convenient tourney-oriented practice.


# usage

```
pip install slider // first time only
python main.py
```